import { Text, View } from'react-native';

//essa é para a discografia
export default function Payment() {
    return (
        <View style={{flex:1, justifyContent: 'center', alignItems: 'center'}}>
            <Text> Discografia</Text>
        </View>
    );
}